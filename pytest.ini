[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = backend
testpaths =
    backend/tests
    backend/apps
python_files =
    test_*.py
    tests.py
    *_tests.py
addopts = -ra -m "not browser"
markers =
    browser: browser smoke tests using pytest-playwright and django live_server
