# Static files directory

This directory contains static files (CSS, JavaScript, images) for the KindEltern Web application.

Structure:

- `css/` - Stylesheets
- `js/` - JavaScript files
- `img/` - Images
